import styled from 'styled-components'

export const StyledNewLibraryForm = styled.div`
  input {
        margin-top: 0;
        margin-left: auto;
    }
    button {
        margin-left: auto;
    }
`